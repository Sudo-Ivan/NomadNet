services:
  nomadnet:
    image: ghcr.io/sudo-ivan/nomadnet:master
    container_name: nomadnet
    user: 65532:65532
    volumes:
      - /path/to/nomadnet-data:/home/nonroot/.nomadnetwork
      - /path/to/reticulum-data:/home/nonroot/.reticulum
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.5'
          memory: 256M
    security_opt:
      - no-new-privileges:true
      - seccomp:unconfined
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    restart: unless-stopped
    networks:
      - nomadnet_network

networks:
  nomadnet_network:
    driver: bridge